{
  "_comment": "This is a TEMPLATE. Do NOT put real secrets here. Download your real OAuth client from Google Cloud Console (APIs & Services > Credentials > Create OAuth client ID > Desktop app), then save it as credentials.json (gitignored) in the project root. The first Google CLI call will open a browser to authorize and write token.json.",
  "installed": {
    "client_id": "YOUR_CLIENT_ID.apps.googleusercontent.com",
    "project_id": "your-gcp-project-id",
    "auth_uri": "https://accounts.google.com/o/oauth2/auth",
    "token_uri": "https://oauth2.googleapis.com/token",
    "auth_provider_x509_cert_url": "https://www.googleapis.com/oauth2/v1/certs",
    "client_secret": "YOUR_CLIENT_SECRET",
    "redirect_uris": ["http://localhost"]
  }
}
