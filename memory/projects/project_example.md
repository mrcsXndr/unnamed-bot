# Project: example

> Example project note. Replace with durable facts about a real project the bot
> helps you run. Notes in `memory/projects/` capture how each project works so
> the bot doesn't re-learn it every session.

- **Repo**: `path or URL`
- **Deploy**: how it ships (command, environment, gotchas)
- **Conventions**: branch naming, commit style, review rules
- **Owners / contacts**: who to loop in
