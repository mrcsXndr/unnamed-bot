# Done sound (optional)

Drop an audio file named `done.m4a` / `done.wav` / `done.mp3` here and the
`Stop` hook will play it when a session ends. Or set `DONE_SOUND` to any path.
No file here = no sound, no error.
